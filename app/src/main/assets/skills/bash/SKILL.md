---
name: bash
description: Execute a shell command (via Termux if installed).
---

# Execute a shell command (via Termux if installed).

## Instructions

Call the un_intent tool with the following exact parameters:

- intent: bash
- parameters: A JSON string with the following fields:
  - command: The bash command to execute. String.
